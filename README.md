# 우리 한글 교실

70~80대 어르신을 위한 기초 한글 학습 웹앱입니다. 휴대폰에서 바로 열어 쓰실 수 있습니다.

**바로가기: https://urihangul.vercel.app/**

## 배우는 내용
- **기초 학습** — 1단계 자음, 2단계 모음, 3단계 소리 합치기(그+아=가), 4단계 받침 읽기
- **글씨 쓰기 연습** — 자음·모음 24자의 쓰는 순서를 움직이는 그림으로
- **글자 한눈에 보기** — 자음·모음을 골라서 소리로 익히기
- **낱말 배우기** — 생활 낱말 761개, 15개 갈래
- **낱말 맞히기 놀이** — 그림과 소리를 듣고 낱말 고르기

모든 글자와 낱말은 성우 음성으로 들려 드립니다.

## 나눠 드릴 자료

- **QR 코드**: [qr.png](qr.png) — 휴대폰 카메라로 찍으면 바로 열립니다
- **인쇄용 안내문(A4)**: [poster.pdf](poster.pdf) — 경로당·복지관 벽에 붙이기 좋습니다

## 인터넷 없이 쓰기

홈 화면 맨 아래 「소리 모두 받기」를 한 번 누르면 음성 1,226개를 휴대폰에
저장해, 인터넷이 없는 곳에서도 그대로 쓸 수 있습니다. 홈 화면에 추가하면
앱처럼 열립니다.

소리는 `audio/audio.bin` **한 덩어리**로 받습니다. 예전에는 1,226개 파일로
따로 저장했는데, 파일마다 붙는 관리 정보 때문에 10.18MB가 실제로는 12.43MB를
차지해 저장공간이 빠듯한 기기에서 일부가 조용히 빠졌습니다. 한 덩어리로 묶어
10.18MB에 들어가고, 받을 때 요청도 1,226번에서 1번이 되었습니다. 원본 mp3를
그대로 이어 붙인 것이라 음질 손실은 없습니다.

재생할 때는 `blob.slice(시작, 끝, 'audio/mpeg')` 으로 필요한 자리만 잘라 씁니다.
묶음을 아직 받지 않았으면 예전처럼 낱개 주소(`audio/aN.mp3`)로 재생합니다.

## 구성

정적 파일만으로 동작합니다. 서버도 빌드 도구도 프레임워크도 없습니다.
모든 경로가 상대경로라 `/hangul/` 아래든 도메인 루트든 그대로 동작합니다.

```
index.html               앱 전체 (HTML+CSS+JS+데이터 인라인)
sw.js                    서비스 워커 (오프라인)
manifest.webmanifest     홈 화면 앱 설정
audio/a0.mp3 … a1225.mp3 성우 음성 1,226개 (인터넷 재생용)
audio/audio.bin          위 1,226개를 이어 붙인 묶음 (내려받기용)
vercel.json              캐시 머리말 설정
tools/                   관리용 스크립트
```

> ⚠️ `window.AUDIO` 와 `window.AUDIO_BIN` 은 `index.html` 안에서 **메인 스크립트보다
> 뒤에** 삽입됩니다. 스크립트 로드 시점에 읽으면 비어 있으니, 반드시 **쓸 때마다
> 새로 읽어야** 합니다.

## 관리용 스크립트

```bash
python tools/build_audio_bundle.py          # audio/*.mp3 -> audio/audio.bin (+ index.html 색인)
python tools/stamp_sw.py                    # index.html 을 고친 뒤 sw.js 캐시 이름 갱신
python tools/regen_qr_poster.py https://주소/ # 주소가 바뀌면 QR·안내문 재발급
python tools/verify.py https://주소/          # 배포된 소리 1,226개 전수 확인
```

`index.html` 을 고쳤다면 **`stamp_sw.py` 를 꼭 돌려 주세요.** 캐시 이름이 그대로면
휴대폰이 옛 화면을 계속 씁니다.
